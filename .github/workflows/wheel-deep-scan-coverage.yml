name: Wheel Deep Scan Coverage

on:
  schedule:
    # Bound scheduled coverage to UTC hours that can overlap the guarded
    # 8 a.m.-4 p.m. New York session. The route applies holidays, early
    # closes, and DST from the versioned exchange calendar.
    - cron: "10,25,40,55 12-21 * * 1-5"
  workflow_dispatch:

jobs:
  deep-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Trigger wheel deep scan coverage
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          WHEEL_DEEP_SCAN_CRON_URL: ${{ secrets.WHEEL_DEEP_SCAN_CRON_URL }}
        run: |
          if [ -z "$WHEEL_DEEP_SCAN_CRON_URL" ]; then
            echo "WHEEL_DEEP_SCAN_CRON_URL secret is required."
            exit 1
          fi

          if [ -z "$CRON_SECRET" ]; then
            echo "CRON_SECRET secret is required."
            exit 1
          fi

          curl --fail-with-body --show-error --silent \
            --request GET \
            --header "Authorization: Bearer $CRON_SECRET" \
            "$WHEEL_DEEP_SCAN_CRON_URL"
